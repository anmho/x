{
  "name": "@x/mcp-observability",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "rm -rf /tmp/mcp-build-check && bun build vercel-observability/server.ts gcp-observability/server.ts --target bun --outdir /tmp/mcp-build-check"
  }
}
