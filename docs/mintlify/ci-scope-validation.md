# CI Scope Validation

This file exists only to validate that a docs-only change triggers the Nx-scoped
`Affected Docs` check while the other stable CI jobs remain present and no-op.

Validation rerun: docs-only change after the final workflow prefilter fix.
